spring:
  datasource:
    url: ${DATASOURCE_URI:jdbc:postgresql://ec2-54-74-156-137.eu-west-1.compute.amazonaws.com:5432/d98almhna1b5s9}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:mhravsyezterfd}
    password: ${DB_PASSWORD:66a7c6f5bb75d08199117dcf3a2dcf4f159ee38be403e511839b8a1fd48b0661}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:https://hivein-registry.herokuapp.com/eureka}
    instance:
      hostname: ${HOSTNAME:hivein-registry}